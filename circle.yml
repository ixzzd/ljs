machine:
  node:
    version: 8.1.2
test:
  override:
    - npm test -- --coverage
  post:
    - npm install -g codeclimate-test-reporter
    - codeclimate-test-reporter < coverage/lcov.info
deployment:
  production:
    branch: master
    commands:
      - ./script/deploy
